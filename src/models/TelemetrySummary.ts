/* tslint:disable */
/* eslint-disable */
/**
 * MailOdds Email Validation API
 * MailOdds provides email validation services to help maintain clean email lists  and improve deliverability. The API performs multiple validation checks including  format verification, domain validation, MX record checking, and disposable email detection.  ## Authentication  All API requests require authentication using a Bearer token. Include your API key  in the Authorization header:  ``` Authorization: Bearer YOUR_API_KEY ```  API keys can be created in the MailOdds dashboard.  ## Rate Limits  Rate limits vary by plan: - Free: 10 requests/minute - Starter: 60 requests/minute   - Pro: 300 requests/minute - Business: 1000 requests/minute - Enterprise: Custom limits  ## Response Format  All responses include: - `schema_version`: API schema version (currently \"1.0\") - `request_id`: Unique request identifier for debugging  Error responses include: - `error`: Machine-readable error code - `message`: Human-readable error description  ## Webhooks  MailOdds can send webhook notifications for job completion and email delivery events. Configure webhooks in the dashboard or per-job via the `webhook_url` field.  ### Event Types  | Event | Description | |-------|-------------| | `job.completed` | Validation job finished processing | | `job.failed` | Validation job failed | | `message.queued` | Email queued for delivery | | `message.delivered` | Email delivered to recipient | | `message.bounced` | Email bounced | | `message.deferred` | Email delivery deferred | | `message.failed` | Email delivery failed | | `message.opened` | Recipient opened the email | | `message.clicked` | Recipient clicked a link |  ### Payload Format  ```json {   \"event\": \"job.completed\",   \"job\": { ... },   \"timestamp\": \"2026-01-15T10:30:00Z\" } ```  ### Webhook Signing  If a webhook secret is configured, each request includes an `X-MailOdds-Signature` header containing an HMAC-SHA256 hex digest of the request body.  **Verification pseudocode:** ``` expected = HMAC-SHA256(webhook_secret, request_body) valid = constant_time_compare(request.headers[\"X-MailOdds-Signature\"], hex(expected)) ```  The payload is serialized with compact JSON (no extra whitespace, sorted keys) before signing.  ### Headers  All webhook requests include: - `Content-Type: application/json` - `User-Agent: MailOdds-Webhook/1.0` - `X-MailOdds-Event: {event_type}` - `X-Request-Id: {uuid}` - `X-MailOdds-Signature: {hmac}` (when secret is configured)  ### Retry Policy  Failed deliveries (non-2xx response or timeout) are retried up to 3 times with exponential backoff (10s, 60s, 300s). 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mailodds.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { TelemetrySummaryTopReasonsInner } from './TelemetrySummaryTopReasonsInner';
import {
    TelemetrySummaryTopReasonsInnerFromJSON,
    TelemetrySummaryTopReasonsInnerFromJSONTyped,
    TelemetrySummaryTopReasonsInnerToJSON,
    TelemetrySummaryTopReasonsInnerToJSONTyped,
} from './TelemetrySummaryTopReasonsInner';
import type { TelemetrySummaryTotals } from './TelemetrySummaryTotals';
import {
    TelemetrySummaryTotalsFromJSON,
    TelemetrySummaryTotalsFromJSONTyped,
    TelemetrySummaryTotalsToJSON,
    TelemetrySummaryTotalsToJSONTyped,
} from './TelemetrySummaryTotals';
import type { TelemetrySummaryRates } from './TelemetrySummaryRates';
import {
    TelemetrySummaryRatesFromJSON,
    TelemetrySummaryRatesFromJSONTyped,
    TelemetrySummaryRatesToJSON,
    TelemetrySummaryRatesToJSONTyped,
} from './TelemetrySummaryRates';
import type { TelemetrySummaryTopDomainsInner } from './TelemetrySummaryTopDomainsInner';
import {
    TelemetrySummaryTopDomainsInnerFromJSON,
    TelemetrySummaryTopDomainsInnerFromJSONTyped,
    TelemetrySummaryTopDomainsInnerToJSON,
    TelemetrySummaryTopDomainsInnerToJSONTyped,
} from './TelemetrySummaryTopDomainsInner';

/**
 * Validation metrics for building dashboards and monitoring.
 * @export
 * @interface TelemetrySummary
 */
export interface TelemetrySummary {
    /**
     * 
     * @type {string}
     * @memberof TelemetrySummary
     */
    schemaVersion?: string;
    /**
     * 
     * @type {string}
     * @memberof TelemetrySummary
     */
    window?: TelemetrySummaryWindowEnum;
    /**
     * 
     * @type {Date}
     * @memberof TelemetrySummary
     */
    generatedAt?: Date;
    /**
     * 
     * @type {string}
     * @memberof TelemetrySummary
     */
    timezone?: string;
    /**
     * 
     * @type {TelemetrySummaryTotals}
     * @memberof TelemetrySummary
     */
    totals?: TelemetrySummaryTotals;
    /**
     * 
     * @type {TelemetrySummaryRates}
     * @memberof TelemetrySummary
     */
    rates?: TelemetrySummaryRates;
    /**
     * Top rejection/status reasons
     * @type {Array<TelemetrySummaryTopReasonsInner>}
     * @memberof TelemetrySummary
     */
    topReasons?: Array<TelemetrySummaryTopReasonsInner>;
    /**
     * Top domains by volume
     * @type {Array<TelemetrySummaryTopDomainsInner>}
     * @memberof TelemetrySummary
     */
    topDomains?: Array<TelemetrySummaryTopDomainsInner>;
}


/**
 * @export
 */
export const TelemetrySummaryWindowEnum = {
    _1h: '1h',
    _24h: '24h',
    _30d: '30d'
} as const;
export type TelemetrySummaryWindowEnum = typeof TelemetrySummaryWindowEnum[keyof typeof TelemetrySummaryWindowEnum];


/**
 * Check if a given object implements the TelemetrySummary interface.
 */
export function instanceOfTelemetrySummary(value: object): value is TelemetrySummary {
    return true;
}

export function TelemetrySummaryFromJSON(json: any): TelemetrySummary {
    return TelemetrySummaryFromJSONTyped(json, false);
}

export function TelemetrySummaryFromJSONTyped(json: any, ignoreDiscriminator: boolean): TelemetrySummary {
    if (json == null) {
        return json;
    }
    return {
        
        'schemaVersion': json['schemaVersion'] == null ? undefined : json['schemaVersion'],
        'window': json['window'] == null ? undefined : json['window'],
        'generatedAt': json['generatedAt'] == null ? undefined : (new Date(json['generatedAt'])),
        'timezone': json['timezone'] == null ? undefined : json['timezone'],
        'totals': json['totals'] == null ? undefined : TelemetrySummaryTotalsFromJSON(json['totals']),
        'rates': json['rates'] == null ? undefined : TelemetrySummaryRatesFromJSON(json['rates']),
        'topReasons': json['topReasons'] == null ? undefined : ((json['topReasons'] as Array<any>).map(TelemetrySummaryTopReasonsInnerFromJSON)),
        'topDomains': json['topDomains'] == null ? undefined : ((json['topDomains'] as Array<any>).map(TelemetrySummaryTopDomainsInnerFromJSON)),
    };
}

export function TelemetrySummaryToJSON(json: any): TelemetrySummary {
    return TelemetrySummaryToJSONTyped(json, false);
}

export function TelemetrySummaryToJSONTyped(value?: TelemetrySummary | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'schemaVersion': value['schemaVersion'],
        'window': value['window'],
        'generatedAt': value['generatedAt'] == null ? value['generatedAt'] : value['generatedAt'].toISOString(),
        'timezone': value['timezone'],
        'totals': TelemetrySummaryTotalsToJSON(value['totals']),
        'rates': TelemetrySummaryRatesToJSON(value['rates']),
        'topReasons': value['topReasons'] == null ? undefined : ((value['topReasons'] as Array<any>).map(TelemetrySummaryTopReasonsInnerToJSON)),
        'topDomains': value['topDomains'] == null ? undefined : ((value['topDomains'] as Array<any>).map(TelemetrySummaryTopDomainsInnerToJSON)),
    };
}

